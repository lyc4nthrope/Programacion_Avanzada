package co.edu.uniquindio.application.models.enums;

public enum Role {
    GUEST,      // Huésped (solo puede reservar)
    HOST,       // Anfitrión (publica alojamientos)
    ADMIN       // Administrador (gestiona plataforma)
}